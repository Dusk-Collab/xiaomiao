# 小淼饭店 · 扫码迁移账户 + 顶部修改密码（本次交付）

## 改了什么
针对你的两个投诉 ——「手机设过密码电脑还让重设」「后台找不到修改密码」—— 补了两块功能：

1. **后台右上角常驻 🔒 修改密码 按钮**：点开即填「原密码 + 新密码」（没绑手机时）或「手机验证码 + 新密码」（绑了手机时）。改成功后出于安全会退出重登，用新密码进即可。
2. **扫码迁移账户**：已在某台设备注册过的账户，可生成「二维码 + 迁移码」（10 分钟有效），在另一台设备登录页扫/粘贴即可把账户搬过去，两台设备共用同一套密码，不用重新注册。
   - 入口：后台「👤 账户与安全 → 📲 迁移账户」，或登录页「已在另一台设备注册？扫码迁移」。

## 技术要点（顺手修掉的真 bug）
- 迁移码签名改用纯 JS FNV（file:// 与 https 结果一致，不再因算法不同验签失败）。
- 本地模式 `hasPhone` 误报导致改密走验证码分支 → 已修正为原密码改密。
- 迁移弹窗曾被登录门遮挡点不到 → z-index 提到 10001（浮于登录门之上）。
- 迁移码压缩到约 147 字符，能装进二维码。

## 验证
- 无头 Chrome 烟雾测试（`_test/smoke.cjs`）全绿：注册 → 顶部改密 → 新密码重登 → 生成迁移码+二维码 → 另一台设备真实点击导入并登录（手机号一致）。
- 线上已校验：部署后的 `admin.html` / `account.js` / `admin-login.js` / `style.css` 均含新功能标记。

## 部署
- main 提交 `b8b3ef4` 已推送；gh-pages 强推 `813c2fb` 已上线。
- 顾客端：`https://dusk-collab.github.io/xiaomiao/customer.html`
- 后台端：`https://dusk-collab.github.io/xiaomiao/admin.html`

## 说明
- 迁移只搬「账户凭证」，不搬店铺商品数据（商品在云端同步里，是另一回事）。
- 本地模式账户仍仅本机有效；要"电脑登录了手机也认"，需填云环境 ID + 部署 auth 云函数 + 开匿名登录（详见 `账户与登录说明.md`）。

## 文件
- `demo/admin.html`、`demo/assets/account.js`、`demo/assets/admin-login.js`、`demo/assets/style.css`（代码）
- `账户与登录说明.md`（用法文档，已增补两节）
- `_test/smoke.cjs`（已 .gitignore，验证脚本）
