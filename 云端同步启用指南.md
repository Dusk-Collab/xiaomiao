# 小淼饭店 · 云端同步启用指南

## 一、当前进度
- ✅ 你的 CloudBase 环境已建好：`xiaomiao-d9grd064qe4237400`（体验版，2027-02-16 到期）
- ✅ auth 云函数已部署并验证后端能跑（账户/登录/改密/验证码逻辑都在）
- ✅ 修复了项目里原有的「GitHub 免费云端同步」通道（之前被一个**未部署的占位代理地址**卡死，已清空，现在走手动粘贴 token 模式）
- ⚠️ **CloudBase 体验版锁死了网页调用云函数**（白名单 + 公网 HTTP 访问都锁），所以"完整云端账户"在免费档下走不通
- ⏳ **唯一卡点**：把修复推上线需要你的一把 GitHub 写令牌（见第二节）

## 二、你需要给我一把 GitHub 令牌（用来推上线）
生成一把 **fine-grained token**（最小权限、最安全）：

1. 打开 https://github.com/settings/tokens?type=beta
2. 点 **Generate new token (fine-grained)**
3. Token name：`xiaomiao-deploy`
4. Resource owner：选 **Dusk-Collab**
5. Repository access：选 **Only select repositories** → **Dusk-Collab/xiaomiao**
6. Permissions → Repository permissions → **Contents: Read and write**
7. Generate token → 复制 `github_pat_...` 发我

我收到后用它把代码推上 GitHub Pages，你的修复**立刻线上生效**。

## 三、启用店铺数据多端同步（上线后你自己做）
1. 打开商家后台 `admin.html` → 左下角「☁ 云端同步设置」
2. 粘贴同一把 `github_pat_...` token（**仅存你本机浏览器 localStorage，不进源码**）
3. 保存并启用 → 店铺菜单 / 公告 / 订单自动多设备实时同步

这把 token 只授权本仓库 Contents 读写，即使泄露也动不了你 GitHub 的其他仓库，安全。

## 四、关于 CloudBase 完整云端账户
auth 云函数已经部署就绪。若将来想启用"网页端云端账户登录"（不止数据同步），需把 CloudBase 体验版**升级到个人版（¥19.9/月）**解封白名单 + HTTP 访问，我再帮你接通网页调用。当前 GitHub 免费方案已足够小店演示多端同步使用。

## 五、体验版到期提醒
CloudBase 环境 `xiaomiao-d9grd064qe4237400` 是**体验版，2027-02-16 到期**。到期后数据从 GitHub / 你手机本地恢复，重开一个新体验版即可（只换环境 ID，零成本）。店铺数据走 GitHub 同步，不锁在云端，随时可全身而退。
