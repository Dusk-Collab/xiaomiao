# 开启「实时同步」指引（网页版 + 小程序版共用）

改完这套代码后，**默认还是你本机各存各的**（和以前一样能用）。
要想实现你要的「我在 A 电脑改了，B 电脑/朋友的手机秒级同步、换电脑进后台画面也一样」，
需要把一个**云端数据库**接上来。步骤如下，照着点就行，不用懂代码。

---

## 一、建一个免费的云端环境（一次性，约 5 分钟）

1. 打开微信，搜索并进入 **「微信云开发」** 小程序（或电脑打开 https://tcb.cloud.tencent.com ）。
2. 用你自己的微信/QQ 登录，新建一个**环境**（个人就能用，有免费额度）。
3. 环境建好后，在控制台「概览」里能看到一串 **环境 ID**（形如 `xiaomiao-7gabc123`），**先复制下来**。

## 二、在后台填上环境 ID（网页版）

1. 用浏览器打开你的后台：`https://dusk-collab.github.io/xiaomiao/admin.html`
2. 第一次会让你**设一个管理员密码**（之后换电脑用这个密码进后台）。
3. 进后台后，左下角点 **「☁ 云端同步设置」**。
4. 把刚才复制的**环境 ID** 粘贴进去，点 **「保存并连接」** → 页面会自动刷新。
5. 刷新后左下角状态：已连接云端 = 成功。

> 这一步之后，**所有打开这个网页的电脑/手机都会连同一个云端数据库**，
> 谁改了菜单/公告/价格，别人那边 1 秒内自动刷新。

## 三、把网页域名加到「安全域名」（关键，否则网页连不上云）

进云开发控制台 → **「设置」→「安全域名」→「Web 安全域名」**，把你的访问地址加进去：
- `dusk-collab.github.io`
（如果是自己绑的域名，就加你自己的域名。）
不加这个，网页版会连不上云端，但本地模式仍能正常使用。

## 四、集合权限（让所有人能读、管理员能写）

云开发控制台 → **「数据库」**，找到 `store` 集合（网页会自动建），把**权限设置**改成：
- 自定义安全规则：`{ "read": true, "write": true }`
（意思是允许网页读写，方便同步。个人小店够用；正式营业后想更严可再加登录校验，见末尾说明。）

## 五、小程序版怎么打通（可选）

`weapp/` 工程里 `app.js` 的 `env` 填**同一个环境 ID**，用微信开发者工具打开即可。
网页版和小程序版会共享同一份店铺数据，菜单/公告改一处、两边同步。

---

## 验证是否成功

1. 用两台设备（或同一台开两个浏览器）都打开顾客端 `customer.html`。
2. 在后台改一个商品名或价格，点保存。
3. 看另一个顾客端 —— **应该 1 秒内自动变成新内容**，不用刷新。
4. 换一台电脑打开后台，输入密码 → 看到的画面和刚才那台一模一样。✅

如果另一台没变：多半是「安全域名」没加、或环境 ID 填错、或那台还没刷新页面。

---

## 你关心的几个点

- **朋友只是顾客**：朋友打开的是顾客端，只能看菜单、点餐，改不了后台。✅
- **后台要密码**：已加。密码存在云端 `config` 文档（加密哈希），换电脑用同一密码进。✅
- **换电脑画面一样**：数据全在云端，任何设备登录后都一致。✅
- **实时**：靠云数据库的 `watch()` 推送，秒级。✅

## 安全补充说明（不必现在管）

现在的密码是「界面级门」+ 集合开放读写，对个人小店够用。若以后要做成面向公众的营业系统，
建议再把集合权限收紧（仅登录用户可写），并用云函数校验密码，避免被人直接调接口改数据。
需要的时候告诉我，我帮你加。
