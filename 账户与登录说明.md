# 商家账户与登录说明

## 你遇到的情况（为什么手机又要重设密码）
之前后台密码只存在**你那台设备的浏览器里**，换一台电脑/手机等于"新设备"，所以又要设一次。这不是 bug，是旧设计把密码存在了本机。

现在改成了**真正的商家账户**：一个手机号 = 一个账户，凭"密码"或"手机号+验证码"登录。

## 现在能用到的功能
- **首次开号**：用手机号 + 密码注册一个商家账户（注册时勾选"本设备 7 天免登录"）。
- **两种登录方式**：
  - 密码登录：手机号 + 密码
  - 验证码登录：手机号 + 短信验证码
- **每次登录都要验证**：没有"记住我"就每次都要输；勾了"7 天免登录"才免。
- **忘记密码**：用绑定手机号或邮箱的验证码重置。
- **账户与安全面板**（后台左下角「👤 账户与安全」）：
  - 绑定 / 换绑**手机号**
  - 绑定**邮箱**（任何邮箱都行，主要用于找回账号）
  - **修改密码**：没绑手机时用原密码改；绑了手机要手机验证码
  - 退出登录

## 🔒 顶部"修改密码"按钮（随时能找到）
后台右上角常驻一个 **🔒 修改密码** 按钮（蓝绿色描边），点它就弹出修改密码框——
- 没绑手机：填「原密码 + 新密码」两遍 → 提交
- 绑了手机：会切换成「手机验证码 + 新密码」，先点"获取验证码"再填
- 改成功后出于安全会退出登录，用**新密码**重新登录即可

> 之前你说"后台找不到修改密码"，现在它就在后台最显眼的位置，不用进面板翻。

## 扫码迁移账户（把账户搬到别的设备）
你已经在一台设备（手机/电脑）注册了账户，想在另一台设备也用同一个账户、又不想重新注册？用"迁移"：

1. **原设备导出**：后台左下角「👤 账户与安全」→「📲 迁移账户」，或直接点登录页的"已在另一台设备注册？扫码迁移"。会显示一张**二维码 + 一串迁移码**，10 分钟内有效。
2. **新设备导入**：在另一台设备的登录页点"已在另一台设备注册？扫码迁移" → 切到"导入"页 → 用手机扫二维码，或把迁移码粘贴进框里 → 点"导入并登录"。
3. 导入成功即自动登录，账户（手机号 + 密码哈希）就搬过来了，**两台设备用同一套密码**。

> 迁移只搬"账户凭证"，不搬店铺商品数据（商品在「☁ 云端同步」里，那是另一回事）。
> 迁移码含密码哈希，**只在你信任的场合、短时间内使用**；过期或不想用了在原设备关掉弹窗即可。若新设备已有别的账户，会提示"请先退出登录再迁移"。

## 两种运行模式（重要）
| 模式 | 触发条件 | 账户存在哪 | 跨设备一致吗 |
|------|----------|-----------|--------------|
| 本地模式 | 没填云环境 ID | 本机浏览器 | ❌ 仅本机 |
| 云端模式 | 填了云环境 ID + 部署了 auth 云函数 | 微信云开发数据库 | ✅ 任意设备 |

> 也就是说：**现在没填云环境时，账户仍只在本机有效**。要"电脑登录了手机也认"，必须走下面的云端模式。

## 开启云端模式（让账户在所有设备通用）
后台「☁ 云端同步设置」里填了环境 ID 之后，还需多做两步（一次性）：

1. **部署 auth 云函数**：在微信开发者工具里，把 `weapp/cloudfunctions/auth` 这个云函数右键"上传并部署（云端安装依赖）"到同一个云环境。
   - 里面已实现：密码 scrypt 加盐哈希、短信/邮箱验证码、会话 token、绑定手机/邮箱、改密、找回。
2. **开启匿名登录**：云开发控制台 → 登录授权 → 把"匿名登录"打开（网页端调用云函数需要它）。
3. **Web 安全域名**：把 `dusk-collab.github.io` 加进云环境的 Web 安全域名白名单（和实时同步是同一个设置）。

做完这 3 步，刷新后台，注册/登录的账户就存在云端了——你换手机、换电脑，用同一个手机号+密码（或验证码）都能进，且画面一致。

## 短信 / 邮箱验证码说明
- 云端模式下，验证码通过**腾讯云短信**和**邮件 SMTP** 真实发送，密钥存在云环境的 `authconfig` 集合里（只有云函数能读，前端读不到）。
- 没配置短信/邮箱密钥时：登录验证码走手机号、找回/绑定走邮箱都会提示"未配置"——这时先在后台「账户与安全」里填好短信/邮箱通道（需登录后填），或暂时只用品密码登录。
- 本地模式（没云端）下：为了让你先体验流程，点"获取验证码"会**直接把演示码弹出来**（toast 提示），方便测试；上线云端后就是真实短信/邮件。

## 安全要点
- 密码在云端用 **scrypt + 每账户随机 salt** 存储，服务端不存明文、前端也拿不到哈希。
- 改密码 / 退出登录会作废其他设备的旧会话，逼其他设备重新登录。
- 验证码 6 位、5 分钟过期、60 秒才能重发、最多试 5 次。
